fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
pydantic-settings==2.5.2
httpx==0.27.2
tenacity==9.0.0
psycopg2-binary==2.9.9

# Geospatial (parcel resolution, area computation)
shapely==2.0.6

# RAG (retrieval)
#
# Windows-only local dev note: chromadb==0.5.15 pins chroma-hnswlib==0.7.6,
# which has no prebuilt Windows/cp312 wheel (only source dist, requiring MSVC
# Build Tools to compile). Linux (Docker, see ./Dockerfile) has manylinux
# wheels for 0.7.6 and installs this file as-is without issue. See
# README.md "Lancer le serveur (dev local)" for the exact 3-command Windows
# workaround (pip still re-validates chromadb's exact pin even once it's
# already installed, so a plain `pip install -r requirements.txt` re-triggers
# the same failure — the requirements line for chromadb must be excluded from
# that final pass).
chromadb==0.5.15
sentence-transformers==3.1.1
mistralai==1.2.3
unstructured[pdf]==0.15.13

# unstructured==0.15.13 does not pin pdfminer.six, so an unconstrained
# `pip install` resolves to whatever is newest on PyPI at install time. Newer
# pdfminer.six releases removed `PSSyntaxError` from `pdfminer.pdfparser`,
# which unstructured's PDF partitioner still imports, breaking with
# `ImportError: cannot import name 'PSSyntaxError'`. Pin to a version
# contemporaneous with unstructured 0.15.13's original release.
pdfminer.six==20240706

# Optional Phase B — DL crop classifier (BreizhCrops-pretrained TempCNN).
# Heavy dependencies: only needed for app/services/dl_service.py and
# scripts/finetune_dl_service.py. The service degrades gracefully
# (source="unavailable") if no checkpoint is present at DL_CHECKPOINT_PATH,
# but these packages must still be importable.
torch==2.4.1
breizhcrops==0.0.4.1
scikit-learn==1.5.2
pandas==2.2.3
tqdm==4.66.5
